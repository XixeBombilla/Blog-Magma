<h1>Lista de Comentarios</h1>

<table>
  <tr>
    <th>Usuario</th>
    <th>Comentario</th>
    <th>Articulo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @coments.each do |coment| %>
  <tr>
    <td><%= coment.usuario %></td>
    <td><%= coment.comentario %></td>
    <td><%= coment.articulo.titulo %></td>
    <td><%= link_to 'Eliminar', coment, confirm: 'Are you sure?', method: :delete, class: "btn" %></td>
  </tr>
<% end %>
</table>
<% content_for :columnaI do%>
<h2>Admin</h2>
<hr class="soften">
<ul>
	<li><%=link_to "Temas", temas_path%></li>
	<hr class="soften">
	<li><%=link_to "Artículos", articulos_path%></li>
	<hr class="soften">
	<li><%=link_to "Comentarios", coments_path%></li>
	<hr class="soften">
	<li><%=link_to "Ir al Sitio", root_path%></li>
</ul>
<br/>
<%= link_to "Salir", destroy_user_session_path, method: :delete, class: "btn" %>
<%end%>
